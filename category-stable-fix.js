/* REALYNK CATEGORY STABLE FIX */
(function(){'use strict';
var filter='',busy=false;
var $=function(id){return document.getElementById(id)};
var esc=function(v){return String(v==null?'':v).replace(/[&<>\"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]})};
function localList(){try{var a=JSON.parse(localStorage.getItem('realynkProperties')||'[]');return Array.isArray(a)?a:[]}catch(e){return[]}}
function all(){var m={};localList().forEach(function(p){if(p&&p.id)m[String(p.id)]=p});return Object.keys(m).map(function(k){return m[k]})}
function match(p){if(!filter)return true;var t=String(p.type||p.listingType||'').toLowerCase(),title=String(p.title||'').toLowerCase();return filter==='Heavy Deposit'?(t.indexOf('heavy')>=0||title.indexOf('heavy deposit')>=0):t===filter.toLowerCase()}
function render(){var h=$('homeList');if(!h||busy)return;busy=true;var q=(($('search')||{}).value||'').trim().toLowerCase();var list=all().filter(function(p){var s=String(p.status||'active').toLowerCase();return ['sold','sold out','soldout','rented out','inactive'].indexOf(s)<0}).filter(match);if(q)list=list.filter(function(p){return [p.title,p.area,p.location,p.type,p.description,p.desc,p.deposit].join(' ').toLowerCase().indexOf(q)>=0});$('filterBar').style.display=filter?'flex':'none';$('filterTitle').textContent=filter?filter+' Properties':'';h.innerHTML=list.length?list.map(function(p){return '<div class="panel property"><h3>'+esc(p.title||'Property')+'</h3><div>📍 '+esc(p.area||p.location||'Location not specified')+'</div><div class="details"><div class="detail"><span>TYPE</span><b>'+esc(p.type||'')+'</b></div><div class="detail"><span>PRICE / RENT</span><b>'+esc(p.price||p.rent||'On Request')+'</b></div></div>'+(p.deposit?'<div style="margin-top:7px"><b>Deposit:</b> '+esc(p.deposit)+'</div>':'')+(p.description||p.desc?'<p>'+esc(p.description||p.desc)+'</p>':'')+'<div class="badge">Broker: '+esc(p.brokerName||p.broker||'Realynk Broker')+'</div></div>'}).join(''):'<div class="empty">No properties found.</div>';busy=false}
function idType(id){return id==='heavyDeposit'?'Heavy Deposit':id.charAt(0).toUpperCase()+id.slice(1)}
function start(){document.addEventListener('click',function(e){var b=e.target.closest&&e.target.closest('#buy,#sale,#rent,#commercial,#heavyDeposit');if(b){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();filter=idType(b.id);document.querySelectorAll('.screen').forEach(function(s){s.classList.toggle('active',s.id==='home')});render();return}var c=e.target.closest&&e.target.closest('#clearFilter');if(c){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();filter='';render();}},true);var h=$('homeList');if(h){new MutationObserver(function(){if(filter&&!busy){clearTimeout(window.__rcTimer);window.__rcTimer=setTimeout(render,0)}}).observe(h,{childList:true})}setInterval(function(){if(filter)render()},1500);setTimeout(render,500)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
