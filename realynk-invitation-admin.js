/* REALYNK INVITATION ADMIN V2 */
(function(){'use strict';if(window.__REALYNK_INVITATION_ADMIN_V2__)return;window.__REALYNK_INVITATION_ADMIN_V2__=true;const ADMIN='seagullairexpress@gmail.com';async function start(){const a=await import('https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js'),au=await import('https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js'),fs=await import('https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js'),cfg=await import('./firebase-config.js');const app=a.getApps().length?a.getApps()[0]:a.initializeApp(cfg.firebaseConfig),auth=au.getAuth(app),db=fs.getFirestore(app);au.onAuthStateChanged(auth,u=>{if(String(u?.email||'').toLowerCase()!==ADMIN)return;let page=document.querySelector('#account .page');if(!page)return;let box=document.getElementById('realynkInvitationAdmin');if(!box){box=document.createElement('div');box.id='realynkInvitationAdmin';box.className='panel';page.appendChild(box)}let invitations=[],applications=[];const render=()=>{let rows=[...invitations.map(x=>({...x,_source:'Invitation'})),...applications.map(x=>({...x,_source:'Application'}))];rows.sort((x,y)=>Number(y.createdAt?.seconds||y.submittedAt?.seconds||0)-Number(x.createdAt?.seconds||x.submittedAt?.seconds||0));let pending=rows.filter(x=>x.status==='pending_verification'||x.status==='pending').length;box.innerHTML='<h3 style="margin:0;color:#0b3768">🤝 Broker Invitations & Applications '+(pending?'• '+pending+' Pending':'')+'</h3><p style="color:#6b7a8c">Invited brokers aur old generic invitations se aaye applications yahan live dikhenge.</p>'+(rows.length?rows.map(x=>'<div style="border:1px solid #dfe6ee;border-radius:12px;padding:12px;margin:10px 0"><b>'+String(x.name||x.fullName||'Broker')+'</b><div style="font-size:13px;color:#6b7a8c">'+String(x.company||x.companyName||'')+' • '+String(x.city||'')+'</div><div style="font-size:13px;color:#6b7a8c;margin-top:4px">'+String(x.phone||'')+' • '+String(x.email||'')+'</div><div style="margin-top:6px"><strong>Status:</strong> '+String(x.status||'Invited')+' <span style="color:#6b7a8c">('+String(x._source)+')</span></div>'+(x.status==='pending_verification'||x.status==='pending'?'<div style="margin-top:8px;color:#18864b;font-weight:800">✓ Pending — Review in Super Admin</div>':'')+'</div>').join(''):'<div style="color:#6b7a8c;padding:12px 0">No broker invitations or applications yet.</div>')};let ready=0;try{fs.onSnapshot(fs.collection(db,'brokerInvitations'),snap=>{invitations=[];snap.forEach(d=>invitations.push({id:d.id,...d.data()}));render() },()=>render())}catch(e){}try{fs.onSnapshot(fs.collection(db,'brokerApplications'),snap=>{applications=[];snap.forEach(d=>applications.push({id:d.id,...d.data()}));render() },()=>render())}catch(e){}render()})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start().catch(console.error)})();