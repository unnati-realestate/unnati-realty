/* REALYNK INVITATION ADMIN V1 */
(function(){'use strict';if(window.__REALYNK_INVITATION_ADMIN_V1__)return;window.__REALYNK_INVITATION_ADMIN_V1__=true;const ADMIN='seagullairexpress@gmail.com';async function start(){const a=await import('https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js'),au=await import('https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js'),fs=await import('https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js'),cfg=await import('./firebase-config.js');const app=a.getApps().length?a.getApps()[0]:a.initializeApp(cfg.firebaseConfig),auth=au.getAuth(app),db=fs.getFirestore(app);au.onAuthStateChanged(auth,u=>{if(String(u?.email||'').toLowerCase()!==ADMIN)return;let page=document.querySelector('#account .page');if(!page)return;let box=document.getElementById('realynkInvitationAdmin');if(!box){box=document.createElement('div');box.id='realynkInvitationAdmin';box.className='panel';page.appendChild(box)}fs.onSnapshot(fs.collection(db,'brokerInvitations'),snap=>{let rows=[];snap.forEach(d=>rows.push({id:d.id,...d.data()}));rows.sort((x,y)=>String(y.createdAt?.seconds||0).localeCompare(String(x.createdAt?.seconds||0)));let pending=rows.filter(x=>x.status==='pending_verification').length;box.innerHTML='<h3 style="margin:0;color:#0b3768">🤝 Broker Invitations '+(pending?'• '+pending+' Pending Verification':'')+'</h3><p style="color:#6b7a8c">Yahan invitation ka live status dikhega.</p>'+(rows.length?rows.map(x=>'<div style="border:1px solid #dfe6ee;border-radius:12px;padding:12px;margin:10px 0"><b>'+String(x.name||'Broker')+'</b><div style="font-size:13px;color:#6b7a8c">'+String(x.company||'')+' • '+String(x.city||'')+'</div><div style="margin-top:6px"><strong>Status:</strong> '+String(x.status||'Invited')+'</div>'+(x.status==='pending_verification'?'<div style="margin-top:8px;color:#18864b;font-weight:800">✓ Accepted — Review in Super Admin</div>':'')+'</div>').join(''):'<div style="color:#6b7a8c;padding:12px 0">No broker invitations yet.</div>')},()=>{});})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start().catch(console.error)})();