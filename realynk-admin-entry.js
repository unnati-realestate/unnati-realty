/* REALYNK SUPER ADMIN ENTRY V5 — single-instance admin trigger */
import { getApps, initializeApp } from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js';
import { getAuth, onAuthStateChanged, GoogleAuthProvider, signInWithPopup } from 'https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js';
import { firebaseConfig } from './firebase-config.js';
if(window.__REALYNK_ADMIN_ENTRY_V5__){}else{
window.__REALYNK_ADMIN_ENTRY_V5__=true;
const ADMIN='seagullairexpress@gmail.com';
const app=getApps().length?getApps()[0]:initializeApp(firebaseConfig);const auth=getAuth(app);const provider=new GoogleAuthProvider();provider.setCustomParameters({login_hint:ADMIN,prompt:'select_account'});let clicks=0,timer=null,loginInFlight=false;
function isAdmin(){return String(auth.currentUser?.email||'').toLowerCase()===ADMIN}
function loadPanel(){if(!isAdmin())return;const old=document.querySelector('script[data-realynk-super-admin]');if(old)return;const s=document.createElement('script');s.type='module';s.dataset.realynkSuperAdmin='1';s.src='./super-admin.js?v=9';document.head.appendChild(s)}
function addEntry(){if(!isAdmin())return removeEntry();const account=document.getElementById('account'),page=account?.querySelector('.page');if(!page||document.getElementById('realynkSuperAdminEntry'))return;const box=document.createElement('div');box.id='realynkSuperAdminEntry';box.style.cssText='margin:14px 0;padding:14px;border:1px solid #f4b400;border-radius:14px;background:#fffaf0';box.innerHTML='<button id="realynkSuperAdminButton" type="button" style="width:100%;border:0;border-radius:11px;padding:14px;background:#fff3c4;color:#7d5b00;font-weight:800;font-size:15px;cursor:pointer">👑 Super Admin / Broker Verification</button><div style="font-size:12px;color:#6b7a8c;margin-top:7px;text-align:center">Admin access only</div>';page.appendChild(box);box.querySelector('#realynkSuperAdminButton').addEventListener('click',()=>{const p=document.getElementById('superAdminPanel');if(p){p.scrollIntoView({behavior:'smooth',block:'center'});return}loadPanel()})}
function removeEntry(){document.getElementById('realynkSuperAdminEntry')?.remove()}
async function hiddenAdminLogin(){if(loginInFlight)return;loginInFlight=true;try{if(isAdmin()){addEntry();loadPanel();return}const result=await signInWithPopup(auth,provider);const email=String(result.user?.email||'').toLowerCase();if(email!==ADMIN){alert('This Google account is not the Realynk Super Admin account.');removeEntry();return}addEntry();loadPanel()}catch(e){console.error('Super Admin login failed',e);if(e?.code!=='auth/popup-closed-by-user')alert('Super Admin login failed: '+(e?.code||e?.message||'Unknown error'))}finally{loginInFlight=false}}
function watchLogo(){if(window.__REALYNK_ADMIN_LOGO_WATCH_V5__)return;window.__REALYNK_ADMIN_LOGO_WATCH_V5__=true;document.addEventListener('click',function(e){const logo=e.target.closest('img[src*="logo.png"], img[alt*="Realynk"], img[alt*="realynk"]');if(!logo)return;clicks++;clearTimeout(timer);timer=setTimeout(()=>{clicks=0},3000);if(clicks>=5){clicks=0;hiddenAdminLogin()}},true)}
onAuthStateChanged(auth,u=>{if(String(u?.email||'').toLowerCase()===ADMIN){addEntry();loadPanel()}else removeEntry()});function start(){watchLogo();addEntry()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();}