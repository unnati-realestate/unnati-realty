/* REALYNK STABILITY V8 - cloud-first posting with persistent broker identity */
import { getApps, initializeApp } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js";
import { getAuth, signInAnonymously, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";
import { getFirestore, collection, doc, setDoc, updateDoc, deleteDoc, onSnapshot, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js";
import { firebaseConfig } from "./firebase-config.js";
const app=getApps().length?getApps()[0]:initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app);
let uid="",brokers={},cloudProperties=[],filter="",authReady=false;
const $=id=>document.getElementById(id);
const esc=v=>String(v??"").replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const digits=v=>String(v||"").replace(/\D/g,"");
const clean=v=>String(v||"").trim().toLowerCase();
const profile=()=>{try{return JSON.parse(localStorage.getItem("realynkBrokerProfile")||"{}")}catch{return {}}};
const toast=m=>{const t=$("toast");if(!t)return;t.textContent=m;t.style.display="block";clearTimeout(window.__rts);window.__rts=setTimeout(()=>t.style.display="none",3000)};
function show(id){document.querySelectorAll(".screen").forEach(s=>s.classList.toggle("active",s.id===id));document.querySelectorAll(".bottom button").forEach(b=>b.classList.toggle("active",b.dataset.nav===id));window.scrollTo(0,0)}
function render(){const q=($("search")?.value||"").trim().toLowerCase();let list=cloudProperties.filter(p=>!filter||String(p.type||"").toLowerCase()===filter.toLowerCase());if(q)list=list.filter(p=>[p.title,p.area,p.type,p.description,p.desc,p.size,p.deposit,p.brokerName].join(" ").toLowerCase().includes(q));const host=$("homeList");if(!host)return;if(!list.length){host.innerHTML='<div class="empty">No properties found.</div>';return}host.innerHTML=list.filter(p=>p.status!=="sold").map(card).join("")}
function card(p){const b=brokers[p.brokerUid]||{};const ph=digits(p.phone||b.phone||p.brokerPhone);const url=location.origin+location.pathname+"?property="+encodeURIComponent(p.id||"");return `<div class="panel property" data-property-id="${esc(p.id)}"><h3>${esc(p.title||"Property")}</h3><div>📍 ${esc(p.area||"Location not specified")}</div><div class="details"><div class="detail"><span>TYPE</span><b>${esc(p.type||"")}</b></div><div class="detail"><span>PRICE / RENT</span><b>${esc(p.price||"On Request")}</b></div></div>${p.deposit?`<div style="margin-top:7px">Deposit: <b>${esc(p.deposit)}</b></div>`:""}${p.size?`<div style="margin-top:7px">Area: <b>${esc(p.size)}</b></div>`:""}<p>${esc(p.description||p.desc||"")}</p><div class="badge">Broker: ${esc(b.fullName||b.agentName||p.brokerName||"Realynk Broker")}</div><div class="rt-actions" style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px">${ph?`<button class="rt-contact-call" data-phone="${ph}" type="button">📞 Call Broker</button><button class="rt-contact-wa" data-phone="${ph}" type="button">💬 WhatsApp</button>`:""}<button class="rt-share" data-url="${esc(url)}" data-title="${esc(p.title||"Realynk Property")}" type="button">↗ Share</button></div></div>`}
function renderMine(){const mine=cloudProperties.filter(p=>isMine(p));const host=$("myList");if(!host)return;$("count")&&($("count").textContent=mine.length);if(!mine.length){host.innerHTML='<div class="empty">Your posted properties will appear here.</div>';return}host.innerHTML=mine.map(p=>`<div class="panel property" data-property-id="${esc(p.id)}"><h3>${esc(p.title||"Property")}</h3><div>📍 ${esc(p.area||"")}</div><div class="details"><div class="detail"><span>TYPE</span><b>${esc(p.type||"")}</b></div><div class="detail"><span>PRICE / RENT</span><b>${esc(p.price||"On Request")}</b></div></div><div style="margin-top:10px"><span class="badge ${p.status==='sold'?'pending':''}">${p.status==='sold'?'SOLD OUT':'ACTIVE'}</span></div><div class="rt-actions" style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px"><button class="rt-edit" data-id="${esc(p.id)}" type="button">✎ Edit</button><button class="rt-status" data-id="${esc(p.id)}" data-status="${p.status==='sold'?'active':'sold'}" type="button">${p.status==='sold'?'🟢 Active':'🔴 Sold Out'}</button><button class="rt-delete" data-id="${esc(p.id)}" type="button">🗑 Delete</button></div></div>`).join("")}
function isMine(p){const me=profile(),myPhone=digits(me.accountPhone||me.phone||""),myEmail=clean(me.agentEmail||me.email||"");const pPhone=digits(p.phone||p.brokerPhone||""),pEmail=clean(p.brokerEmail||p.email||"");if(String(p.brokerUid||"")===String(uid))return true;if(myPhone&&pPhone&&myPhone===pPhone)return true;if(myEmail&&pEmail&&myEmail===pEmail)return true;const brokerList=Object.values(brokers||{});const myCloud=brokerList.find(b=>{const bp=digits(b.accountPhone||b.phone||""),be=clean(b.email||b.agentEmail||"");return (myEmail&&be&&myEmail===be)||(myPhone&&bp&&myPhone===bp)});if(myCloud){const cp=digits(myCloud.accountPhone||myCloud.phone||""),ce=clean(myCloud.email||myCloud.agentEmail||"");if(cp&&pPhone&&cp===pPhone)return true;if(ce&&pEmail&&ce===pEmail)return true}try{const local=JSON.parse(localStorage.getItem("realynkProperties")||"[]");return Array.isArray(local)&&local.some(x=>String(x?.id)===String(p.id)&&x?.mine)}catch{return false}}
function waitForAuth(ms=8000){if(auth.currentUser&&!auth.currentUser.isAnonymous)return Promise.resolve(auth.currentUser);return new Promise(resolve=>{let done=false;const finish=u=>{if(done)return;done=true;clearTimeout(timer);unsub&&unsub();resolve(u&& !u.isAnonymous?u:null)};const unsub=onAuthStateChanged(auth,u=>{if(u&&!u.isAnonymous)finish(u)});const timer=setTimeout(()=>finish(null),ms);if(auth.currentUser&&!auth.currentUser.isAnonymous)finish(auth.currentUser)})}
async function getReadUser(){if(auth.currentUser)return auth.currentUser;try{const u=(await signInAnonymously(auth)).user;return u||null}catch(e){console.warn("Realynk read auth",e);return null}}
async function post(){const p=profile();if(!p.agentName){toast("Please create your Broker Account first");show("account");return}const owner=await waitForAuth();if(!owner){toast("Please sign in to your Broker Account first");show("account");return}const title=$("title")?.value.trim();if(!title){toast("Please enter Property Title");$("title")?.focus();return}const id=String(Date.now())+"-"+Math.random().toString(36).slice(2,7);const brokerEmail=owner.email||p.agentEmail||"",brokerPhone=digits(p.accountPhone||p.phone||"");const data={id,title,area:$("area")?.value.trim()||"",type:$("type")?.value||"Rent",price:$("price")?.value.trim()||"",deposit:$("deposit")?.value.trim()||"",size:$("size")?.value.trim()||"",description:$("desc")?.value.trim()||"",phone:digits($("phone")?.value)||brokerPhone,brokerUid:owner.uid,brokerName:owner.displayName||p.agentName,brokerPhone,brokerEmail,brokerKey:digits(brokerPhone)||clean(brokerEmail),status:"active",createdAt:serverTimestamp(),updatedAt:serverTimestamp()};
for(let attempt=1;attempt<=3;attempt++){try{await setDoc(doc(db,"properties",id),data);toast("Property posted successfully");["title","area","price","deposit","size","desc"].forEach(x=>{if($(x))$(x).value=""});show("dashboard");return}catch(e){console.error("Realynk property post attempt "+attempt,e);if(attempt===3){const code=String(e?.code||"");toast(code.includes("permission")?"Firebase permission blocked property post":"Property could not be saved");return}await new Promise(r=>setTimeout(r,1000))}}}
function edit(id){const p=cloudProperties.find(x=>String(x.id)===String(id));if(!p)return;if(!isMine(p)){toast("You can only edit your own property");return}show("post");["title","area","price","deposit","size","desc"].forEach((x,i)=>{if($(x))$(x).value=[p.title,p.area,p.price,p.deposit,p.size,p.description||p.desc||""][i]||""});if($("type"))$("type").value=p.type||"Rent";window.__editId=id;const btn=$("submit");if(btn)btn.textContent="Update Property"}
async function saveEdit(){const id=window.__editId;if(!id)return post();const p=cloudProperties.find(x=>String(x.id)===String(id));if(!p||!isMine(p)){toast("You can only edit your own property");return}try{await updateDoc(doc(db,"properties",id),{title:$("title").value.trim(),area:$("area").value.trim(),type:$("type").value,price:$("price").value.trim(),deposit:$("deposit").value.trim(),size:$("size").value.trim(),description:$("desc").value.trim(),updatedAt:serverTimestamp()});window.__editId=null;$("submit").textContent="Submit Property";toast("Property updated successfully");show("dashboard")}catch(e){console.error(e);toast(String(e?.code||"").includes("permission")?"Firebase permission blocked update":"Update failed")}}
async function removeProperty(id){const p=cloudProperties.find(x=>String(x.id)===String(id));if(!p||!isMine(p)){toast("This property is not linked to your broker account");return}if(!window.confirm("Delete this property listing?"))return;try{await deleteDoc(doc(db,"properties",String(id)));toast("Property deleted successfully");window.dispatchEvent(new Event("realynkPropertyChanged"))}catch(e){console.error("Realynk cloud delete failed",e);toast(String(e?.code||"").includes("permission")?"Firebase permission blocked delete":"Delete failed — property was not removed")}}
async function toggleStatus(id,status){const p=cloudProperties.find(x=>String(x.id)===String(id));if(!p||!isMine(p)){toast("You can only change your own property");return}try{await updateDoc(doc(db,"properties",id),{status,updatedAt:serverTimestamp()});toast(status==='sold'?"Property marked Sold Out":"Property is Active again")}catch(e){toast(String(e?.code||"").includes("permission")?"Firebase permission blocked status":"Status update failed")}}
function share(url,title){if(navigator.share){navigator.share({title,text:title,url}).catch(()=>{})}else if(navigator.clipboard){navigator.clipboard.writeText(url).then(()=>toast("Property link copied"))}else{prompt("Copy property link",url)}}
function bind(){const stop=e=>{e.preventDefault();e.stopImmediatePropagation()};["buy","sale","rent","commercial"].forEach(id=>{const b=$(id);if(!b||b.dataset.stab)return;b.dataset.stab="1";b.addEventListener("click",e=>{stop(e);filter=id[0].toUpperCase()+id.slice(1);show("home");render()},true)});$("clearFilter")?.addEventListener("click",e=>{stop(e);filter="";$("search").value="";render()},true);$("search")?.addEventListener("input",()=>render(),true);$("submit")?.addEventListener("click",e=>{stop(e);window.__editId?saveEdit():post()},true);$("postQuick")?.addEventListener("click",e=>{stop(e);show("post")},true);$("add")?.addEventListener("click",e=>{stop(e);show("post")},true);$("brokerPost")?.addEventListener("click",e=>{stop(e);show("post")},true);document.addEventListener("click",async e=>{const t=e.target.closest("button");if(!t)return;if(t.classList.contains("rt-contact-call")){e.preventDefault();location.href="tel:+91"+t.dataset.phone}else if(t.classList.contains("rt-contact-wa")){e.preventDefault();location.href="https://wa.me/91"+t.dataset.phone}else if(t.classList.contains("rt-share")){e.preventDefault();share(t.dataset.url,t.dataset.title)}else if(t.classList.contains("rt-edit")){e.preventDefault();edit(t.dataset.id)}else if(t.classList.contains("rt-delete")){e.preventDefault();removeProperty(t.dataset.id)}else if(t.classList.contains("rt-status")){e.preventDefault();toggleStatus(t.dataset.id,t.dataset.status)}},true)}
function start(){bind();getReadUser().then(u=>{if(u&&!u.isAnonymous)uid=u.uid;onSnapshot(collection(db,"brokers"),s=>{brokers={};s.forEach(d=>brokers[d.id]=d.data());renderBrokers();renderMine()});onSnapshot(collection(db,"properties"),s=>{cloudProperties=[];s.forEach(d=>cloudProperties.push({...d.data(),id:d.id}));render();renderMine()})}).catch(console.warn);onAuthStateChanged(auth,u=>{authReady=true;if(u&&!u.isAnonymous){uid=u.uid;renderMine()}})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
window.realynkPropertyEdit=edit;
window.realynkPropertyDelete=removeProperty;
window.realynkPropertyStatus=toggleStatus;
